import React from "react";

const SectionTitle = ({ title }) => {
  return (
    <div>
      <h4 className="section-title  text-[#BABABA] text-[11px] mt-[40px] mb-3 uppercase">
        {title}
      </h4>
    </div>
  );
};

export default SectionTitle;
